###############################################################
###	if you use the APPLICATION_ENV you should add .htaccess 
###	to the .gitignore file to ensure the error display does not
###	execute in your production environment
### this is turned on by default...dependant on your apache settings
### you can set this in the virtual host definition instead 
###############################################################
SetEnv APPLICATION_ENV "DEV"

###############################################################
###	some basic http caching if you're going to server static content
###############################################################

#<FilesMatch "\.(gif|jpe?g|png|js|css)$">
#	FileETag All
#	ExpiresActive On 
#	ExpiresDefault "access plus 2 week" 
#	Header append Cache-Control "public, must-revalidate"
#	AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
#</FilesMatch>